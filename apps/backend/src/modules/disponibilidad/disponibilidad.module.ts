import { Module } from '@nestjs/common';

/** Contenido real en la tarjeta "Backend: Módulo Disponibilidad — CRUD + validación de traslapes de horario". */
@Module({})
export class DisponibilidadModule {}
